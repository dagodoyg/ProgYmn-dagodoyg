#pragma once
#include <iostream>
#include <cmath>
#include <vector>
#include <numeric>
#include <algorithm>

double vector_norm(std::vector<double> v);
std::vector<double> poly_deriv(std::vector<double> v);