#pragma once
#include <iostream>
#include <cmath>
#include <vector>
#include <numeric>
#include <algorithm>

std::vector<double> poly_deriv(std::vector<double> v);
double poly_evaluate(std::vector<double> v, double x);